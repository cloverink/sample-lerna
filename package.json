{
  "name": "sample-lerna",
  "private": true,
  "workspaces": [
    "packages/*",
    "documents/*"
  ],
  "scripts": {
    "start": "lerna run start --scope example-app",
    "build": "lerna run build --scope helloworld"
  },
  "devDependencies": {
    "lerna": "5.1.2"
  }
}
